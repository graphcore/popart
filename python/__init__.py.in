import os, sys

__version__ = "@VERSION@"

# Add the DSO library path
lp = os.path.join(os.path.dirname(os.path.abspath(__file__)), "../../lib")
lp = os.path.abspath(lp)
sys.path.insert(0, lp)

# Import all symbols into our namespace
from popart_core import *
from popart.builder import Builder
from popart.session import InferenceSession, TrainingSession, PrepareDeviceException
from popart.options import SessionOptions
from popart.tensorinfo import TensorInfo
